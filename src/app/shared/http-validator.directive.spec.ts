import { HttpValidatorDirective } from './http-validator.directive';

describe('HttpValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
